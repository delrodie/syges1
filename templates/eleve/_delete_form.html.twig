<form method="post" action="{{ path('eleve_delete', {'id': eleve.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élève?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.id) }}">
    <div class="pull-right"><button class="btn btn-danger">Supprimer</button></div>
</form>
