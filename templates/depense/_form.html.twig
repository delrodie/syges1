{{ form_start(form) }}
    <div class="row mt-4">
        <div class="col-12 mb-4">{{ form_row(form.date) }}</div>
        <div class="col-12 mb-4">{{ form_row(form.description) }}</div>
        <div class="col-12 mb-4">{{ form_row(form.montant) }}</div>
    </div>
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>

{{ form_end(form) }}
